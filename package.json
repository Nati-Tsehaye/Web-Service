{
  "name": "bingo-websocket-server",
  "version": "1.0.0",
  "description": "Dedicated WebSocket server for the Bingo game",
  "main": "dist/server.js",
  "scripts": {
    "build": "rm -rf dist && tsc && echo '--- Build Output Verification ---' && ls -la dist/",
    "start": "node dist/server.js",
    "dev": "ts-node-dev --respawn src/server.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@upstash/redis": "^1.31.0",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5"
  }
}
